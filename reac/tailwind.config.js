/** @type {import('tailwindcss').Config} */
export const content = [
  './src/**/*.{js,jsx,ts,tsx}', // Ensure Tailwind scans your React files for classes
];
export const theme = {
  extend: {},
};
export const plugins = [];
